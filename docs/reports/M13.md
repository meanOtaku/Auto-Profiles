# M13 Implementation Report — Optional UI

- Milestone: M13 — Optional UI
- Date: 2026-08-01
- Status: **Implementation drafted; verification deferred by owner**

## Scope delivered

- `ui/dashboard.html`: a self-contained static dashboard (profiles, candidates with approve/reject, recent events) that is purely an `/api/v1` REST client, no build step, no domain logic.
- `UIConfig` (disabled by default) and a `GET /` route in `api/app.py`, only registered when `ui.enabled` is true.

## Explicit exclusions

No WebSocket live-event wiring in the dashboard itself (manual REST refresh only); no settings-editing form beyond what the API already exposes via `curl`/other clients. Both are documented scope reductions given remaining session budget, not defects.

## Commands executed

- `ruff format`, `ruff check .`, `mypy src tests`: all passed (66 source files).
- Smoke test via `TestClient`: `GET /` with `ui.enabled=true` returns 200 and valid HTML containing the dashboard.
- Automated `pytest` and the full quality/packaging matrix were **not run**, per the owner's explicit request.

## Verification status and limitations

No test counts or production-readiness claims are made. The dashboard's `fetch()` calls have not been exercised in a real browser against a live `face-profile serve` process in this session — only the static route's HTML delivery was verified via `TestClient`.

## Next closure steps

1. Manually verify the dashboard in a real browser against a running `face-profile serve` instance.
2. Add WebSocket live-event wiring to the dashboard.
3. Run the repository quality and packaging matrix.
4. Mark M13 complete only after those results are recorded.
